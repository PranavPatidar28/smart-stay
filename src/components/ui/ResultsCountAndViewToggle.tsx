import React from "react";

const ResultsCountAndViewToggle = () => {
  return (
    <div className="p-2 bg-gray-100 rounded flex items-center justify-between">
      <span className="text-gray-500">Results count: --</span>
      <span className="text-gray-400">View toggle coming soon</span>
    </div>
  );
};

export default ResultsCountAndViewToggle; 